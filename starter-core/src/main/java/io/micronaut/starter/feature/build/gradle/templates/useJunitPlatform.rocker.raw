@import io.micronaut.starter.sdk.gradle.GradleDsl;
@args(GradleDsl dsl)

@if(dsl == GradleDsl.KOTLIN) {
tasks.withType<Test> {
    useJUnitPlatform()
}
} else {
tasks.named('test') {
    useJUnitPlatform()
}
}